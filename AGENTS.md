# 🔧 Gateway development

1. Bump the gateway patch version in `Cargo.toml` and `Cargo.lock` in every commit landing on `master`, including documentation-only commits, and include the resulting `vX.Y.Z` in the commit title.
